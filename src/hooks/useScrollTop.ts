import React from 'react';

// export const useScrollTop = () => {
//   const { pathname } = useResolvedPath('/');

//   React.useEffect(() => {
//     window.scrollTo(0, 0);
//   }, [pathname]);

// };